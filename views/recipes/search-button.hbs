<h2>Search Results for "{{searchQuery}}"</h2>
{{#if recipes}}
  <ul>
    {{#each recipes}}
      {{#if this}}
        <li>{{title}}</li>
      {{/if}}
    {{/each}}
  </ul>
{{else}}
  <p>No results found.</p>
{{/if}}